# Flask Backend

A minimal Flask application for Pharma Agentic AI.

## Setup

1. Install dependencies:
```bash
pip install -r requirements.txt
```

2. Run the application:
```bash
python app.py
```

The server will start on `http://localhost:8000`

## Endpoints

- `GET /` - Home endpoint
- `GET /health` - Health check endpoint
